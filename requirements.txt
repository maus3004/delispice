# delispice_app — core runtime dependencies (cross-platform).
# Pinned to the versions verified working on the dev machine.
#
# This file runs the app either as a desktop window or a web server. Do not
# add pywebview (desktop-only) or gunicorn (server-only) here — those live in
# requirements-desktop.txt and deploy/requirements-server.txt respectively.
#
# NOTE: pip picks the correct platform wheel for each package automatically.
# Do NOT pin polars-runtime-* — installing polars pulls the right runtime
# variant for the target CPU (a Mac-selected runtime would be wrong on Linux).
dash==4.4.0
duckdb==1.5.4
plotly==6.8.0
polars==1.40.1
numpy==2.4.4
scipy==1.17.1
scikit-learn==1.8.0
pyarrow==24.0.0
Flask==3.1.3
Werkzeug==3.1.8
requests==2.34.2   # data_pipeline/height_scraper.py (Baseball Reference)
